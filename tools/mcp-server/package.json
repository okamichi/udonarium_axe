{
  "name": "@udonarium-axe/mcp-server",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Session-scoped MCP control of a dedicated Udonarium Axe browser",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "start": "node dist/server.js",
    "test": "npm run build && node --test test/*.test.mjs"
  },
  "engines": {
    "node": ">=22"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "1.30.0",
    "playwright": "1.63.0",
    "zod": "4.5.4"
  },
  "devDependencies": {
    "typescript": "6.0.3",
    "@types/node": "25.9.2"
  },
  "imports": {
    "#mcp/*": "./dist/*"
  }
}
